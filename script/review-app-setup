# Env setup vars

# Get an auth token
heroku auth:token

# Set the BASE_HOST and ACTION_CABLE_URL from the review app name
heroku config:set BASE_HOST=https://${HEROKU_APP_NAME}.herokuapp.com ACTION_CABLE_URL=wss://${HEROKU_APP_NAME}.herokuapp.com/cable --app ${HEROKU_APP_NAME}
