<div id="react-root"></div>

<script>
  window.collectionData = <%= @collection.serialized_for_test_survey.to_json.html_safe %>;
  window.nextAvailableId = <%= @next_submission_test ? @next_submission_test.id : 'null' %>;
  window.noneAvailable = <%= params[:action] == 'completed' %>;
  window.filestackToken = <%= FilestackFile.filestack_security(read_only: true).to_json.html_safe %>
</script>

<%= javascript_pack_tag 'test-survey-app' %>
