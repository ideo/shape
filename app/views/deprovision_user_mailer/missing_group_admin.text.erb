<%= @user.email %> was deprovisioned, and now the group: "<%= @group.name %>" has no admin.  No organization admins were available to set as new group admins.

The group is probably in a somewhat broken state.
