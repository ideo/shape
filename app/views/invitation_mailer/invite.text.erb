Hello<%= " #{@user.name}" if @user.name.present?%>,

<% if @application.present? %>
    You've been invited to join <%= @org_name %>'s <%= @application_name %> account <% if @group_name.present? %>to view <%= @group_name %>’s results<% end %>
<% else %>
  <%= @invited_by.name %> has invited you to join<%= @org_name ? " #{@org_name}'s" : '' %> "<%= @invited_to_name %>"<%= @org_name ? ' group ' : '' %>on Shape.
<% end %>

<%= @url %>
