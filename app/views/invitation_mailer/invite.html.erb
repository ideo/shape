<div class='p'>
  Hello<%= " #{@user.name}" if @user.name.present? %>,
</div>

<div class='p'>
  <%= @application.invite_message %>
  <br/>

  <div class='email-link'>
    <%= link_to @application.invite_cta, @url %>
  </div>
</div>

<div class='p'>
  Or copy and paste this link into your browser: <%= @url %>
</div>
