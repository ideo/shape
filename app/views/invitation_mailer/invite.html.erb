<div class='p'>
  Hello<%= " #{@user.name}" if @user.name.present?%>,
</div>

<div class='p'>
  <%= @invited_by.name %> has invited you to join
  <%= @org_name ? "#{@org_name}'s" : '' %>
  "<%= @invited_to.name %>" <%= @org_name ? 'group' : '' %> on Shape.
  <br/>

  <div class='email-link'>
    <%= link_to "Join #{@invited_to_type}", @url %>
  </div>
</div>

<div class='p'>
  Or copy and paste this link into your browser: <%= @url %>
</div>
